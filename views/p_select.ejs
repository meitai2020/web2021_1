<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>野手一覧</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><a href="/db/id/<%= row.id %>"><%= row.id %></a></td>
            <td><%= row.player_name %></td>
            <td><%= row.name2 %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>