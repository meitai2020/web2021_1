<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>野手一覧</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.player.player_name %></td>
            <td><%= row.team.team_name %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>